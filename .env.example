# Example environment variables for enabling AI providers
# Do NOT commit your real API keys. Copy this to .env and fill values locally.

# Default AI provider used by StudyHub/AI features.
# To enable the Claude Haiku 4.5 provider, set this to 'claude-haiku-4.5'.
VITE_AI_PROVIDER=claude-haiku-4.5

# (Optional) Claude API key - required if you enable Claude provider.
# Place your real key in a local .env (not committed to version control).
VITE_CLAUDE_API_KEY=

# (Optional) Model name override for Claude if needed.
VITE_CLAUDE_MODEL=claude-haiku-4.5

